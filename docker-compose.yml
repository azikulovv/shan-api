services:
  shan-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shan-backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    command: sh -c "yarn prisma migrate deploy && node dist/src/server.js"


