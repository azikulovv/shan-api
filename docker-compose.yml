services:
  shan-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shan-backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env.docker
    volumes:
      - shan_backend_data:/app/data

volumes:
  shan_backend_data:
